<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>eu.debooy.maven</groupId>
  <artifactId>pom</artifactId>
  <version>1.0.0</version>
  <packaging>pom</packaging>
  <name>DOOS Super POM</name>
  <organization>
    <name>Marco de Booij</name>
    <url>http://www.debooy.eu</url>
  </organization>
  <licenses>
    <license>
      <name>European Union Public Licence (EUPL v.1.1)</name>
      <url>http://ec.europa.eu/idabc/eupl</url>
      <distribution>repo</distribution>
    </license>
  </licenses>
  <scm>
    <connection>scm:svn:http://svn.debooy.eu/svn/projects/SuperPOM/tags/pom-1.0.0</connection>
    <developerConnection>scm:svn:http://svn.debooy.eu/svn/projects/SuperPOM/tags/pom-1.0.0</developerConnection>
  </scm>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <distributionManagement>
    <repository>
      <id>DOOS</id>
      <name>DOOS releases</name>
      <url>http://intra.debooy.eu/nexus/content/repositories/doos-releases</url>
    </repository>
    <snapshotRepository>
      <id>DOOS</id>
      <name>DOOS snapshots</name>
      <url>http://intra.debooy.eu/nexus/content/repositories/doos-snapshots</url>
    </snapshotRepository>
    <site>
      <id>Documentatie</id>
      <name>DOOS Documentatie</name>
      <url>scp://intra.debooy.eu/var/www/site/SuperPOM</url>
    </site>
  </distributionManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-assembly-plugin</artifactId>
          <configuration>
            <displayName>${pom.name}</displayName>
            <description>${pom.description}</description>
            <descriptorRefs>
              <descriptorRef>jar-with-dependencies</descriptorRef>
            </descriptorRefs>
             <archive>
              <manifest>
                <packageName>${pom.groupId}</packageName>
              </manifest>
              <manifestEntries>
                <Build-Time>${buildNumber}</Build-Time>
                <Implementation-Title>${pom.name}</Implementation-Title>
                <Implementation-Vendor>${pom.organization.name}</Implementation-Vendor>
                <Implementation-Vendor-Id>${pom.groupId}</Implementation-Vendor-Id>
                <Implementation-Version>${pom.version}</Implementation-Version>
                <Specification-Title>${pom.name}</Specification-Title>
                <Specification-Vendor>${pom.organization.name}</Specification-Vendor>
                <Specification-Version>${pom.version}</Specification-Version>
              </manifestEntries>
            </archive>
          </configuration>
          <executions>
            <execution>
              <id>make-assembly</id>
              <phase>package</phase>
              <goals>
                <goal>single</goal>
              </goals>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-ejb-plugin</artifactId>
          <version>2.1</version>
          <configuration>
            <ejbVersion>3.0</ejbVersion>
            <archive>
              <manifest>
                <addClasspath>true</addClasspath>
              </manifest>
              <manifestEntries>
                <Build-Time>${buildNumber}</Build-Time>
                <Implementation-Title>${pom.name}</Implementation-Title>
                <Implementation-Vendor>${pom.organization.name}</Implementation-Vendor>
                <Implementation-Vendor-Id>${pom.groupId}</Implementation-Vendor-Id>
                <Implementation-Version>${pom.version}</Implementation-Version>
                <Specification-Title>${pom.name}</Specification-Title>
                <Specification-Vendor>${pom.organization.name}</Specification-Vendor>
                <Specification-Version>${pom.version}</Specification-Version>
              </manifestEntries>
            </archive>
          </configuration>
        </plugin>
        <plugin>
          <artifactId>maven-jar-plugin</artifactId>
          <configuration>
            <displayName>${pom.name}</displayName>
            <description>${pom.description}</description>
            <archive>
              <manifestEntries>
                <Build-Time>${buildNumber}</Build-Time>
                <Implementation-Title>${pom.name}</Implementation-Title>
                <Implementation-Vendor>${pom.organization.name}</Implementation-Vendor>
                <Implementation-Vendor-Id>${pom.groupId}</Implementation-Vendor-Id>
                <Implementation-Version>${pom.version}</Implementation-Version>
                <Specification-Title>${pom.name}</Specification-Title>
                <Specification-Vendor>${pom.organization.name}</Specification-Vendor>
                <Specification-Version>${pom.version}</Specification-Version>
              </manifestEntries>
            </archive>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-war-plugin</artifactId>
          <version>2.1-alpha-2</version>
          <configuration>
            <displayName>${pom.name}</displayName>
            <description>${pom.description}</description>
            <archive>
              <manifest>
                <addClasspath>true</addClasspath>
                <classpathPrefix>lib/</classpathPrefix>
              </manifest>
              <manifestEntries>
                <Build-Time>${buildNumber}</Build-Time>
                <Implementation-Title>${pom.name}</Implementation-Title>
                <Implementation-Vendor>${pom.organization.name}</Implementation-Vendor>
                <Implementation-Vendor-Id>${pom.groupId}</Implementation-Vendor-Id>
                <Implementation-Version>${pom.version}</Implementation-Version>
                <Specification-Title>${pom.name}</Specification-Title>
                <Specification-Vendor>${pom.organization.name}</Specification-Vendor>
                <Specification-Version>${pom.version}</Specification-Version>
              </manifestEntries>
            </archive>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <source>1.6</source>
          <target>1.6</target>
          <!--  encoding>UTF-8</encoding -->
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-help-plugin</artifactId>
        <version>2.1.1</version>
      </plugin>
      <plugin>     
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-release-plugin</artifactId>
        <version>2.0-beta-9</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <!-- configuration>
          <encoding>UTF-8</encoding>
        </configuration -->
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-site-plugin</artifactId>
        <version>2.1.1</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>2.6</version>
        <configuration>
          <forkMode>pertest</forkMode>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>buildnumber-maven-plugin</artifactId>
        <version>1.0-beta-3</version>
        <executions>
          <execution>
            <phase>validate</phase>
            <goals>
              <goal>create</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <format>{0,date,yyyy-MM-dd HH:mm:ss}</format>
          <items>
            <item>timestamp</item>
          </items>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <reporting>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-project-info-reports-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-report-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>findbugs-maven-plugin</artifactId>
        <version>2.1</version>
        <configuration>
          <threshold>Normal</threshold>
          <effort>Default</effort>
          <excludeFilterFile>findbugs-exclude.xml</excludeFilterFile>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>taglist-maven-plugin</artifactId>
        <version>2.4</version>
        <configuration>
          <tags>
            <tag>TODO</tag>
          </tags>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-pmd-plugin</artifactId>
        <configuration>
          <linkXref>false</linkXref>
          <!--  sourceEncoding>UTF-8</sourceEncoding -->
          <minimumTokens>100</minimumTokens>
          <targetJdk>1.6</targetJdk>
        </configuration>
      </plugin>
    </plugins>
  </reporting>

  <dependencies>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.6</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
